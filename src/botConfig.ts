import { ColorResolvable } from 'discord.js';

const botConfig = {
	color: {
		default: '#FF2235' as ColorResolvable,
		red: '#D83C3E' as ColorResolvable,
		green: '#3BA55C' as ColorResolvable,
	},
};

export default botConfig;
